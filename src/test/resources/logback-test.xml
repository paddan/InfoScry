<?xml version="1.0" encoding="UTF-8"?>
<!--
  Tests must not write into the user's data directory, and a passing test run should print nothing.
  Tests that need the real file pipeline (redaction, JSON shape, rolling limits) build their own
  Logback context in the test itself.
-->
<configuration>

  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="WARN">
    <appender-ref ref="CONSOLE"/>
  </root>

</configuration>
