# SDD ledger — plan: docs/superpowers/plans/2026-09-22-infoscry-continuation.md

## Preflight interface and consistency scan — 2026-09-22

| Tasks | Shared file or interface | Finding / ruling |
|---|---|---|
| 1 → 3 | `LlmRequest` and both provider adapters | Task 1 adds a nullable forced-tool choice. Task 3 must leave it absent for ordinary Ask requests. No conflict. |
| 1 → 4 | `LlmStreamingClient` and measured tool capability | Task 1 makes the persisted measurement deterministic; Task 4 correctly requires `true` before provider contact. No conflict. |
| 2 → 3–5 | rebuild maintenance and readers | Task 2 adds coverage only and does not change the public search contract. No conflict. |
| 3 → 4 | `RequestBudget` and `CitationValidator` | Task 3 produces the exact shared contracts Task 4 consumes. No conflict. |
| 3–4 → 5 | accepted API/SSE contracts | Task 5 is downstream and must not create parallel contracts. No conflict. |
| 1 | task text, files, tests, and commit | The forced-tool field and three CLI states are sufficient and named consistently. No conflict. |
| 2 | task text, files, tests, and commit | It permits a production seam only when indispensable, preserving YAGNI. No conflict. |
| 3 | task text, files, tests, and commit | It matches original Task 21 scope and explicit request-budget rules. No conflict. |
| 4 | task text, files, tests, and commit | It matches original Task 22 scope and depends on Task 3 as stated. No conflict. |
| 5 | task text and original plan | It delegates exact detailed scope to accepted original Tasks 23–28 and adds no product behavior. No conflict. |

Ruling: Task 1 may proceed in the isolated worktree from `5a21de9`; the uncommitted documentation update remains in the primary checkout and is not part of this code task. Cost if wrong: the agent could update stale status text, so the task brief explicitly excludes documentation changes.

Task 1: BLOCKED in `/Users/patrik/.codex/worktrees/llm-probe/infoscry` — the managed worktree reverted each agent write to `5a21de9`, so it could not retain a patch, report, or commit. Ruling: retry Task 1 in the writable primary checkout while preserving its four pre-existing documentation changes; the worktree mechanism cannot provide usable isolation here. Cost if wrong: accidental overlap with documentation is possible, so the retry is restricted to the Task 1 Kotlin/test files and must report every changed path.

Task 1: fix round 1/5 (3 addressed, 0 open — CLI wire request, invalid required tool, ordinary tool omission; commits `7dd2f37`..`a295b1c`)
Task 1: complete (commits `5a21de9`..`a295b1c`, review clean)

Task 2: BLOCKED before implementation — `CollectionService.deleteConfirmed` calls `MutationCoordinator.withExclusiveMaintenance`, whose `maintenanceOrder` waits behind a rebuild. That contradicts the binding design rule that new mutating API/CLI commands return `MAINTENANCE_IN_PROGRESS`/423 while only existing background stages wait at checkpoints.

Ruling: expand Task 2 to modify `MutationCoordinator.withExclusiveMaintenance` so a competing external exclusive owner fails fast with `MaintenanceInProgressException`; retain `awaitMutation` for queued background stages. This implements design spec lines 803–808 and original plan lines 510 and 1046–1050. Cost if wrong: changing the wrong path could reject existing durable job stages instead of letting them resume safely, so direct coordinator tests must distinguish the two paths.

Task 2: review findings — Critical: import route and remote CLI enqueue durable work without mutation admission during rebuild; Important: the lease test holds an empty lease and never proves an old reader can search after swap.

Ruling: expand Task 2 to gate import enqueueing at the HTTP and foreground/CLI command boundaries before `jobs.enqueue`, preserving `awaitMutation` only for already claimed job stages. Expand `LuceneIndex.withReaderLease` so the test can perform a genuine old-generation search after publication. Cost if wrong: gating after enqueue would leave a durable job despite the 423 response, and a lease seam exposing more than the leased reader would become an unneeded production API.

Task 2: fix round 1/5 (2 addressed, 0 open — import admission before persistence, genuine old-reader search after swap; commits `f01bc58`..`80e011c`)
Task 2: complete (commits `a295b1c`..`80e011c`, review clean)

Task 3: review findings — Critical: no durable Ask persistence and no required citation correction; Important: buffered SSE, invented provider budget, unstable CLI JSON, and missing service-level contract tests.
Task 3: fix round 1/5 review — correction is still streamed rather than non-streaming; no AskService outbound-call tests; provider overflow loses `CONTEXT_BUDGET_EXCEEDED`; durable audit lacks evidence IDs/invalid markers and usage/cost totals.

Ruling: expand Task 3 with a provider-neutral non-streaming completion operation and a new post-004 migration/store update. The original Task 21 explicitly requires one non-streaming correction and persisted evidence/citation/model-call/usage data; changing existing migration `004_llm.sql` would break installed databases. Cost if wrong: a broad completion API could become an unbounded alternate LLM path, so it is correction-only in Ask and must preserve the same provider envelope/budget rules.

Task 3: fix rounds 2-5 addressed durable correction audit, provider-envelope correction budgeting, per-call usage/cost, and stable CLI/SSE contracts (commits `5e2f0a6`..`059f4ca`).

Task 3: independent review at `b932dfb` found - Critical: CLI printed the uncorrected streamed answer; Important: evidence could close the `<evidence>` frame and correction evidence sat in a `system` turn, `ContextPacker` permanently skipped same-document hits after a failed new document, and `prompt_version`/`retrieval_snapshot` were placeholders.

Ruling: fix round 6 addresses all four plus the dead `jsonEvent` mapping, adds delimiter/near-duplicate/backfill/correction/persistence tests, and files what is intentionally left (correction-overflow discards the initial answer rather than persisting it). Cost if wrong: escaping document text changes the bytes an LLM sees, so it is limited to `&`, `<`, `>` and covered by a test.

Task 3: complete (commits `80e011c`..HEAD, review findings fixed; full `check` green at 690 JVM tests + 5 frontend tests, 0 skipped).

Task 4a: complete (worker `openrouter/xiaomi/mimo-v2.6-flash`, commit `279f619`, 14 focused tests green; contract recorded in `task-4a-report.md`). The worker also fixed an inherited budget bug: `bounded()` measured leftover bytes against a payload that already embedded the full text.

Task 4b: attempt 1 (`291f43b5`) failed with provider 400 `tool messages must include a non-empty string tool_call_id` after thousands of spurious tool calls (non-existent tool names); attempt 2 (`79f4d3ae`) stopped by the operator. No repository changes from either attempt. `task-4b-brief.md` now carries a tool-discipline section; resume with a fresh worker run.

Task 4b: attempt 3 dispatched (worker `openrouter/deepseek/deepseek-v4-flash-0731`, thinking high, fresh context, run `ab42ea5a`; BASE `6c4264c`). Brief updated: HEAD reference corrected to `6c4264c`, and a mandatory "Tool discipline" section added (real tools only, no invented names like `glob`, one read per file, batched shell calls, no exploration loops) — address the two prior provider-side failures.

Task 4b: Ruling — the provider-neutral `LlmMessage(role, content)` cannot carry an assistant tool-call message or a tool result, so the Task 22 tool loop cannot be expressed against the accepted LLM contract (verified: `LlmClient.kt:14`, `OpenAiCompatibleClient.kt:284/369`, `AnthropicClient.kt:313/383`). This is the same defect that produced the attempt-1 provider 400. Ruling: extend the one existing neutral contract with additive optional tool-call/tool-result fields mapped in both adapters, keeping Ask, the Ask correction call, and the capability probe byte-identical; reject the alternative of sending tool results as plain user-role text. Authority: design spec lines 536-552 require the budget to count tool calls/results and to omit whole call/result groups; plan Task 22 Step 4 states the same. Cost if wrong: the neutral LLM contract used by Ask is touched, so adapter wire tests for the unchanged plain-message path must prove no regression; if they cannot, the change is reverted and Task 22 returns to the plan. Authorized commit scope expansion: `llm/LlmClient.kt`, both provider adapters, and their tests.

Task 4b: attempt 3 FAILED — timed out at 30 min (`1800000ms`) with 47 tool calls, all `read`/`grep`/`ls`/`find`, and zero files written. No repository changes (only this ledger file). Root cause: the worker read ~30 files before writing anything, then spent its final 6+ minutes in one open assistant generation that never completed, so the wall clock expired before any code existed. It did correctly identify the `LlmMessage` tool-exchange gap (see the ruling above) — the analysis was right, the execution plan was wrong.

Task 4b: Ruling — stop dispatching Task 4b as one slice. Three consecutive attempts on the saved worker model have produced no code, and the binding constraint is task size plus unbounded pre-reading, not model capability (the same model completed the comparably sized Task 4a). Split Task 4b into four sequential commits, each with a named file set, a pre-supplied interface digest in the brief so the worker needs almost no discovery, an explicit instruction to write the first file within its first few tool calls, and a per-slice `timeoutMs` plus `checkpointBeforeDeadlineMs`. Cost if wrong: more dispatches and four commits instead of one, and the slices must not create interfaces that differ from the brief's accepted contract, so each slice review checks the contract against the Task 4a report and the plan.

Task 4b-1: dispatched (worker `openrouter/deepseek/deepseek-v4-flash-0731`, thinking high, fresh context, run `b9ca2c5f`; BASE `6c4264c`). Brief `task-4b1-brief.md` pre-supplies the verified interface digest (all file:line call sites), the exact `LlmMessage` contract, the OpenAI/Anthropic wire requirements, the byte-identical unchanged path, the duplicate-envelope extraction from `RequestBudget.kt:51-52`, and the required test cases, so the worker needs almost no discovery. Hard wall clock `timeoutMs=2400000` with `checkpointBeforeDeadlineMs=300000` and attention notices at 10 turns / 80k tokens, so a stall surfaces early instead of at the deadline.

Task 4b-1: in progress and behaving as designed — 9 reads of exactly the brief-named files in the first 30 seconds, then edits to the three test files, a red run, the implementation edits (`LlmMessage`, both adapters, `RequestBudget` shared envelope builder), a second test run, and test-XML verification. Seven target files modified. Contrast with attempt 3: 47 reads, zero writes, deadline kill.

Task 4b-3: operator authorization recorded — Patrik approved changing the worker model for the `InvestigationService` slice only ("ja byt worker för 4b-3 till standard modell"). Scope: 4b-3 only; 4b-1, 4b-2, and 4b-4 stay on the saved worker model `openrouter/deepseek/deepseek-v4-flash-0731`. Exact model id still to be confirmed with the operator before dispatch; the saved worker override in `/Users/patrik/.pi/agent/settings.json` is not being edited for this.
Task 4b-3: model confirmed by operator — `openrouter/deepseek/deepseek-v4-pro`, applied only to the 4b-3 dispatch (passed as the dispatch `model` argument, not written to the saved worker override).

Task 4b slice split: Ruling (revises the four-slice split) — collapse persistence into the service slice, giving three slices: 4b-1 (LLM contract, done at `2713896`), 4b-2 (`InvestigationService` + migration 006 + store persistence + tests), 4b-3 (`InvestigationRoutes` + 422 gate + route test). Why: `004_llm.sql` already ships `conversations`, `messages` (role includes `tool`), `model_calls`, `citations` and `usage_totals`, so the durable *message/model-call* half of Task 22 needs no new schema; what remains — tool-call activity, the monotonic evidence-ID mapping, each request's eligible evidence, per-request omitted groups, and limit events — is schema design that is inseparable from the service that writes it. A separate persistence slice would have forced the controller to design the schema, which is the work being delegated. Cost if wrong: the heaviest slice carries schema plus service, so the dispatch gets a larger wall clock and an instruction to commit the migration first, the store second, and the service third, so a timeout loses at most the last increment.

Task 4b-1: complete (commit `6c4264c`..`2713896`, review clean). Reviewer verdicts: spec compliance ✅ PASS (all 15 requirements with file:line evidence), task quality ✅ PASS, no Critical or Important findings. It independently confirmed the two claims that mattered: exactly one `ChatCompletionRequest(`/`AnthropicMessagesRequest(` construction per provider (adapter and `RequestBudget` share them), and zero pre-existing test assertion lines modified. Verified evidence: `LlmClient.kt:14-31` (extended contract plus all three invariants enforced in `init`), `OpenAiCompatibleClient.kt:342-350/423-439`, `AnthropicClient.kt:412-453`, `RequestBudget.kt:50-52`; 82 tests, 0 failures/errors/skipped.

Task 4b-1: minor (deferred): `AnthropicClientTest.kt:412-414` proves "content is a string" via `.jsonPrimitive` rather than an explicit `!is JsonArray` assertion, so a regression would surface as a `ClassCastException` instead of a clear message.
Task 4b-1: minor (deferred): `RequestBudgetTest.kt:33` covers the tool-exchange measurement only through the Anthropic provider; there is no OpenAI counterpart.
Task 4b-1: minor (deferred): the error message in the consolidated `toolParametersJson` changed wording from "a tool definition carried invalid parameters JSON" to "a tool carried invalid parameters JSON"; no test depends on it.
Task 4b-1: minor (deferred, controller-observed): the no-profile `RequestBudget.measure(request)`/`fit`/`requireFits` overloads still use the synthetic `ProviderFrame` (`RequestBudget.kt:39/64-77`), which does not carry `toolCalls`/`toolCallId` and would therefore under-count a tool exchange. Not a regression from this task (the path predates it and is reachable only via `ContextPacker.kt:42/67` when `profile == null`, i.e. tests), and the 4b-2 brief forbids Investigate from using those overloads. Deferred to the final review to triage whether the synthetic frame should carry tool exchanges.

Task 4b-2: controller brief defect caught and corrected mid-run — the brief named `src/test/kotlin/infoscry/storage/LlmStoreTest.kt` as the store test to modify, but the real file is `src/test/kotlin/infoscry/llm/LlmStoreTest.kt` (package `infoscry.llm`; the `infoscry.storage` directory holds the other store tests, which is what misled the digest). The worker followed the brief and created a duplicate `LlmStoreTest` class in `infoscry.storage`. Steered it to move the new tests into the existing file, delete the duplicate, and use the `infoscry.llm.LlmStoreTest` filter; the brief file is corrected so re-reads and the review see the right path. Cost of the defect: one steer round plus a little rework; the alternative (letting two same-named store-test classes land) would have confused every later reader and the final review.

Task 4b-2: child asked for a supervisor decision at the start of increment 3 (`InvestigationService`). It reported two compounding problems: it had been generating Kotlin through bash heredocs and producing typos (including a misspelled file `InvestigtionService.kt`), and the component itself is large (loop, per-call budgeting through the profile overload only, whole-group pruning, evidence-id monotonicity, round/call limits, repeated-call detection, limit events). It offered to cut scope to a "minimal" service. Ruling: refuse the scope cut — all nine binding requirements and the full test list stand, because the review rejects a slice for anything missing — and instead fix the mechanism. Directive: delete the misspelled file; `write` the service as a compile-only skeleton with `TODO` bodies in one call; `write` the complete test file in one call; commit both immediately so the test target and the type contract survive a wall-clock stop; then implement the bodies in three committed slices (loop + limits; budgeting + pruning + evidence ids; citation correction + persistence + `usage_totals`). Cost if wrong: a skeleton commit adds a commit that review may find uninteresting, and if the model still cannot finish, the slice lands as a compiling partial with unimplemented TODOs that the review will flag as not-done — which is why the child was told to report DONE_WITH_CONCERNS rather than claim success.

Task 4b-2: controller check at ~21:48. Increments 1 and 2 and the safety-net skeleton/test commit are in (`57b55aa`, `fd38044`, `8e15263`), and the misspelled file is gone; `InvestigationService.kt` is 411 lines and was uncommitted. Two defects steered: (1) the committed `InvestigationServiceTest.kt` holds only 4 tests and covers one of the brief's eleven required cases (only "irreducible budget overflow makes zero provider calls"), so the review would fail spec compliance; (2) 411 uncommitted lines at two thirds of the wall clock. Directive: commit the implementation partial immediately, then add the ten numbered missing cases one or two at a time with their implementation, committing as it goes, and report DONE_WITH_CONCERNS enumerating which items are implemented, tested, or missing if the clock runs out. Cost if wrong: the child may spend its remaining time on tests rather than a finished slice, in which case the slice is resumed from its persisted session to complete the remaining cases — cheaper than losing the run.

Task 4b-2: first report came back DONE_WITH_CONCERNS (5 commits: `57b55aa`, `fd38044`, `8e15263`, `4e957a5`, `8f848cc`). Controller verification rather than trust, because the report is full of typos (`ColletionId`, `Bollean`, `InvestigatRequest`, `invetigate`): code state verified good — `InvestigationService.kt` compiles, focused run is 118 tests / 0 failures / 0 errors / 0 skipped, and the implementation really does contain the round and call limits (`MAX_ROUNDS = 10`, `MAX_TOOL_CALLS = 20`), budget measurement through the profile overload before each call, one budgeted citation correction via `LlmCompletionClient`, monotonic `S<n>` evidence ids, and real `toolCalls`/`toolCallId` messages. Tests: 7, covering 4 of the brief's 11 cases.

Task 4b-2: review-round-1 findings dispatched by resuming the same child (new run `6594cde7`, session reused, fresh wall clock). Two are functional gaps, not test-only: **F1** pruning is entirely absent — `006` creates `request_omissions` and the snapshot carries `eligibilityEvidenceIds`, but nothing prunes oldest completed user turns then oldest whole tool-call/result groups, so brief requirement 3 is unimplemented; **F2** the 30 s tool, 2 min provider-inactivity and 10 min per-turn timeouts are absent. Plus eight missing tests: cross-collection NOT_FOUND and no path/collection in schemas; 10-hit and adjacent clamps; 21-call and 11-round limits; cancellation before the next provider call; one correction for `[S999]` then persisted invalid markers; evicted evidence not re-accepted without re-supply while older citations still open; group-preserving pruning with no id renumbering in a small context window; and tool exchanges asserted as real `toolCalls`/`toolCallId` on the captured outbound request. Also required: run `check` to completion instead of reporting it as timed out, and stop using bash heredocs for Kotlin. Cost if wrong: the slice is already large and this round may not finish it, in which case the remaining work is scoped to the missing test list rather than the whole service, and the tree stays compiling either way.

Task 4b-2 fix round 1: child asked for a supervisor decision after entering a corruption cycle — every attempt to add F1/F2 by editing the 430-line `InvestigationService.kt` (edit tool, bash heredoc, or Python string surgery) mangled identifiers, while the committed `4e957a5` still compiles with its 7 tests. It proposed (a) a full 400-line from-scratch rewrite with `write`, (b) accepting the state with F1/F2 unimplemented plus the eight missing tests, or (c) Python string surgery. Ruling: reject (b) because F1 is binding brief requirement 3 and would fail the review on spec compliance; reject (c) because Python and heredocs are the two mechanisms that already produced the corruption; authorize (a) only as a last resort with a `git checkout --` recovery. Chosen mechanism: move the new logic out of the fragile file — a new small `InvestigationBudgetPruner.kt` holding the pure pruning function and a new `InvestigationTimeouts.kt` holding the three deadlines, leaving only two minimal `edit` calls in the service, each followed by `compileKotlin`, with `git checkout --` as the recovery and a commit after each green step. This is an authorized extra file so the review does not read it as scope creep. Rationale: writing a fresh small file is the only operation that has worked reliably in this task (migration, store methods, skeleton and tests all landed cleanly that way), so it converts a fragile large-file edit into a small-file write plus a two-line call site. Cost if wrong: one extra file in the package that a reviewer may find unnecessary, and if the call-site edits still corrupt the service, the fallback is the from-scratch rewrite with the compiling commit one command away.

Task 4b-2 fix round 1 (cont.): child delivered F1 via the new-file mechanism (`ca6e825` budget pruning + omission recording) and the tool-call timeout (`3b17c78`), leaving the provider-inactivity timeout and all eight tests. Second decision requested: approve one-test-per-edit-call in the existing test file, or accept a partial. Ruling: approve the incremental approach but change the unit of work to small NEW test files written with `write` (the only mechanism that has worked in this task), and forbid further edits to the existing `InvestigationServiceTest.kt`. For F2 the provider-inactivity timeout I supplied the exact mechanism from verified code: the collect site is a single line (`InvestigationService.kt:158`), so rewrite the small `InvestigationTimeouts.kt` with a `collectWithProviderDeadline(flow, onEvent)` helper and change only that line's prefix, which leaves every brace where it was — that brace restructuring is what broke the previous attempts. Coroutines version verified as `1.10.2`; `withTimeout` gives a total per-call bound, `flow.timeout` gives true between-events inactivity if it compiles without a forbidden preview opt-in, and the child must state which it used. Cost if wrong: the timeout may be a total per-call bound rather than a true inactivity bound, which is stricter than the brief and must therefore be visible in the report for the review to judge.

Task 4b-2 fix round 1 (cont.): controller caught a regression in the child's **staged but uncommitted** change to `InvestigationService.kt` — `git diff --cached` was 1 insertion / 9 deletions, and the deletions were exactly the `withTimeout(TOOL_CALL_TIMEOUT_MS)` block plus its `TOOL_TIMEOUT` limit event that `3b17c78` had already committed and greened. `InvestigationTimeouts.kt` was still only the three constants with no `collectWithProviderDeadline` helper, so nothing implemented provider inactivity either. Steered it before commit: restore from `3b17c78` if the wrapper was the problem, finish a single refactor that keeps both bounds behind one helper, verify with `git show --stat` that the commit deletes nothing from `3b17c78`, and confirm `git diff` shows no removed timeout before committing. This is the value of polling the child's actual worktree state rather than reading its prose: the report would have said "F2 partial", while the staged tree had regressed F2 from partial to absent.

Task 4b-2 fix round 1 (cont.): second controller intervention, from the child's own bash log rather than its prose. Two defects found. (1) It "restored" the service from `ca6e825`, which is the pruning commit that *predates* the tool timeout, so the working tree and index currently have no `withTimeout` and no `TOOL_TIMEOUT` event while `HEAD` (`3b17c78`) has them — the regression I warned about had already landed in the working tree. Directed it to `git checkout HEAD -- src/main/kotlin/infoscry/investigate/InvestigationService.kt` and verify with grep before anything else. (2) The real blocker is behavioural: on a 158-error compile it deleted the test file and restored instead of fixing errors. In Kotlin a single misspelled type in a shared fake cascades to every use, so 158 errors is typically two or three typos. New procedure, directed literally: never delete a file for failing to compile; read only the first error; fix that one identifier with a small `edit` reusing spellings from the file's own imports; recompile; expect two to four iterations. Also told it to stop retyping commands — the log shows `./gradew` and `sdd`/`sdf` from a mangled `$(asdf where java)`, each costing a turn. Cost if wrong: the child spends its remaining clock iterating on typos rather than finishing the tests, in which case the slice closes as a compiling partial with an exact numbered remainder.

Task 4b-2 fix round 1: run `6594cde7` failed on wall clock after partial progress. Committed before it died: `32e6241` (the `collectWithProviderDeadline` helper in `InvestigationTimeouts.kt`). It also correctly reverted the tool-timeout regression (working tree again has `TOOL_CALL_TIMEOUT_MS`, verified by grep). Not done: the helper is **not wired** into the service (`grep collectWithProviderDeadline InvestigationService.kt` is empty), the KDoc references a `ProviderInactivityException` type that does not exist anywhere, and none of the eight tests landed.

Task 4b-2 fix round 1: model-reliability finding, and the real blocker. The child's last artifact was an untracked `IvestigationLimtsTst.kt` (filename itself corrupted). Controller bookkeeping: renamed it to `InvestigationLimitsTest.kt` and ran `compileTestKotlin` to measure it rather than guess. Result: 52 compile errors, and the errors are purely mangled identifiers — `InestigationTools`, `InestigationToos`, `InestigationService`, `nvestigateRequest`, `romptService`, `tolls`, `FakeSearch`, `FakePersistence`. Roughly ten corrupted identifiers in a 132-line file, each losing a leading character or transposing letters. The same model produced clean code at small sizes in this task (the 2,596-byte `InvestigationBudgetPruner.kt`, the migration, the store methods, and the 325-byte `InvestigationTimeouts.kt`), so the failure scales with generated length rather than with task difficulty. The corrupted file was deleted (untracked, unusable) and is recorded here.

Task 4b-2: escalation decision requested from the operator. Evidence assembled: two wall clocks consumed by run `a9cba0b7` and its revival `6594cde7`; the remaining delta is small and mechanical (wire the committed helper at `InvestigationService.kt:158` plus a typed expiry, delete the dangling `ProviderInactivityException` KDoc reference, and add the service-level tests), yet the approved Pro worker cannot emit reliable multi-line Kotlin. Options put to the operator: (a) approve a stronger worker model for this remainder only, (b) keep the Pro model and split the remainder into per-test dispatches of roughly 50 lines each targeting the scale at which it does succeed, or (c) accept 4b-2 as a compiling partial (F1 plus the tool timeout, provider inactivity implemented but unwired) and carry the tests into slice 4b-3. Recommendation: (a) for the test files, because the same corruption will hit the routes and route tests in 4b-3 otherwise, and the slice is otherwise complete.

Task 4b-2: Ruling (scope of required service tests) — the brief's Step 1 listed cross-collection `NOT_FOUND` with no path/collection parameter in the tool schemas, plus the 10-hit search clamp and the `before`/`after` adjacent clamps, among the service-test cases. Those behaviours live in the tools, and `src/test/kotlin/infoscry/investigate/InvestigationToolsTest.kt` already covers all four of them with 14 passing tests from Task 4a (the 4a report documents 999→10 and 0→1 clamps, 99/99→5+anchor+5 and negative→0 adjacent clamps, and a schema assertion over every tool's serialized parameters). Re-testing them at the service layer would duplicate coverage rather than verify the orchestration. Ruling: the service test file covers the six orchestration cases only (round/call limits, cancellation before the next provider call, one correction for `[S999]` with persisted invalid markers, evicted evidence not re-accepted without re-supply, group-preserving pruning with no id renumbering, and tool exchanges asserted as real `toolCalls`/`toolCallId` on the captured outbound request), and the tool-level cases are cited from the existing file. Cost if wrong: if the review wants end-to-end coverage of a clamp through the service, it will raise it and the test can be added there; the underlying behaviour is demonstrably covered either way.

Task 4b-2: controller bookkeeping — deleted the corrupted untracked `IvestigationLimtsTst.kt`/`InvestigationLimitsTest.kt` (52 compile errors, all mangled identifiers) after measuring it, since leaving an unusable untracked file would confuse the next worker and the review. This is file bookkeeping, not a code fix: no committed source was touched, and the last good state remains `32e6241`.

Task 4b-2: operator approved option (a) — a stronger worker model for this remainder only. Escalation dispatched: worker `openrouter/anthropic/claude-opus-4.8:high`, fresh context, run `a6ccde25`, BASE `32e6241`, brief `task-4b2-fix-brief.md`. The brief pins the exact one-line wiring change at `InvestigationService.kt:158` (prefix only, so no brace moves), the typed `PROVIDER_TIMEOUT` handling mirroring the existing tool-timeout pattern, the unused `TURN_TIMEOUT_MS` bound that still needs implementing, and the six tests to insert one at a time into the existing test file reusing its fakes. The saved worker override in `/Users/patrik/.pi/agent/settings.json` is untouched; the model applies to this dispatch only.

Task 4b-2: escalation attempt 1 FAILED before doing any work — the approved stronger model `openrouter/anthropic/claude-opus-4.8` was rejected by the platform with HTTP 402 `payment_required`: "You requested up to 128000 tokens, but can only afford 41647" (`limit_source: openrouter_credits`, request `gen-1790143345-TQg6d1bO8TgF8HLcADRs`). No repository changes; tree verified clean at `32e6241`. This is a platform credit limit, not a code or lane failure, and it implies roughly three dollars of affordable output remains, which is a planning risk for the remaining work (4b-3 routes plus Tasks 23-28 UI, e2e and release). Switched to a cheaper but still strong model within the operator's approval to change the worker for this remainder.

Task 4b-2: escalation attempt 2 dispatched — worker `openrouter/anthropic/claude-sonnet-5:high`, fresh context, run `278ef57b`, BASE `32e6241`, same fix brief. Saved worker override in `/Users/patrik/.pi/agent/settings.json` remains untouched.

Task 4b-2: operator feedback — the escalated models are too expensive; keep the saved worker model `openrouter/deepseek/deepseek-v4-flash-0731`. Recorded and applied. I stopped the running Gemini 2.5 Pro dispatch (`b5c22dc7`) mid-flight to stop the spend, and inspected what it had produced rather than discarding it: its partial diff was exactly Task A done correctly — `InvestigationService.kt:158` now calls `collectWithProviderDeadline`, a `catch (TimeoutCancellationException)` clause emits a typed `PROVIDER_TIMEOUT` error plus its `LimitEventSnapshot`, and the dangling `ProviderInactivityException` KDoc reference is corrected. Controller verified it independently with `./gradlew compileKotlin` = BUILD SUCCESSFUL, so that work is retained rather than re-generated.

Task 4b-2: escalation attempts 1 and 2 both failed on platform credits with no repository change — `openrouter/anthropic/claude-opus-4.8` affordable 41,647 of the requested 128,000 tokens, `openrouter/anthropic/claude-sonnet-5` affordable 104,119 of 128,000 (`payment_required`, `limit_source: openrouter_credits`). The binding constraint is the model's declared 128k max_tokens against a small remaining balance, so premium models are unusable until credits are added. Recorded as a planning risk for the remaining work (4b-3 routes, then Tasks 23-28 UI, e2e, CI and packaging), and reported to the operator.

Task 4b-2: finish dispatch `d1510490` (worker `openrouter/deepseek/deepseek-v4-flash-0731:high`, fresh context, BASE `32e6241`) with brief `task-4b2-finish-brief.md`, which supersedes `task-4b2-fix-brief.md` and records the verified starting state so the worker does not re-derive Task A. It pins the per-turn bound as three exact edits with line anchors (`withTimeout(TURN_TIMEOUT_MS)` opened after `try {` at line 106, closed before the catch chain at line 425, plus a new `TURN_TIMEOUT` catch clause ordered before the generic ones) and explicitly forbids re-indenting the enclosed body, because that brace and indentation surgery is what corrupted the file twice before. Task C stays one test per edit with a compile and a commit after each.

Task 4b-2 finish run `d1510490`: Task A committed as `568451e` and Task B as `de32997` (the per-turn bound is in place: `withTimeout(TURN_TIMEOUT_MS)` opened after `try {` at line 106, closed before the catch chain, with a `TURN_TIMEOUT` catch clause ordered ahead of the generic ones). Then the child spent ~25 minutes and 25 tool calls on reconnaissance without writing a single test — reading the loop, timing a full test run and grepping JUnit XML. Same exploration pattern that killed two earlier runs, so steered with the recipes instead of waiting: for `MAX_TOOL_CALLS`, one stream carrying 21 `ToolCallReady` events with all-different `arguments` so the repeat detector does not swallow them; for `MAX_ROUNDS`, one stream with a single `ToolCallReady` (the identical key is a repeat from round 2, so `totalToolCalls` stays low while `roundCount` climbs to 11). Both recipes were derived by reading the actual loop rather than guessed, and the only new fake permitted is a provider call counter. One note for the eventual review: a full `./gradlew test` run in this repository takes about 7m43s because the test task pulls the frontend install and build, so wall clock is scarcer here than it looks.

Task 4b-2 finish run `d1510490`: timed out at 45 minutes but landed two tests first, uncommitted. Controller verified them rather than trusting the report: `git diff` shows `a never-ending tool-call stream stops at the MAX_TOOL_CALLS limit` (21 distinct-argument `ToolCallReady` events, asserting the `MAX_TOOL_CALLS` error) and `a tool-calling turn stops at the MAX_ROUNDS limit` (single repeated call plus an assertion that the provider was streamed at most 11 times), and `./gradlew compileTestKotlin` succeeds. Nine tests now exist in the file. Root cause of the timeout: the reconnaissance habit plus the wall-clock cost of the test task — `processResources` pulls the frontend build, so the first full test run of a session takes about 8 minutes. Steered accordingly.

Task 4b-2 fix round: resumed as `8455cb03` with instructions to commit the two verified tests first, then add the remaining four cases one at a time under the narrow filter `--tests 'infoscry.investigate.InvestigationServiceTest'`, committing after each green step. Recipes supplied for each: capture outbound `LlmRequest`s to assert real `toolCalls`/`toolCallId` on the follow-up call; a `completeCalls` counter to prove exactly one correction for `[S999]`; cancel a `launch`ed collector and assert the provider stream count stops growing; and for evicted evidence, that no outbound request leaves a `toolCalls` message without its matching `tool` result and that evidence ids never renumber. The child was explicitly told that if a case is not observable through the public event or snapshot contract it must report that finding instead of writing a test that asserts nothing.

Task 4b-2 finish run `8455cb03`: timed out at 45 minutes but committed three more tests, and the previous resume's two landed as `794ab16`. Controller-verified test inventory in `InvestigationServiceTest.kt` (13 tests, tree clean): `a never-ending tool-call stream stops at the MAX_TOOL_CALLS limit`, `a tool-calling turn stops at the MAX_ROUNDS limit`, `tool calls travel as real toolCalls messages with matching tool results` (`c26ea31`), `an answer citing a missing evidence id gets exactly one correction and stays invalid` (`2b6507e`), `cancelling mid-turn stops further provider calls` (`9064733`), `pruning never splits a tool-call group from its results` (`d2f8b90`), plus the seven pre-existing tests. Six of the seven required cases are therefore covered by committed tests.

Task 4b-2 fix round: resumed a second time as `702a87e6` for the one remaining case (evidence evicted by pruning must not be accepted as a valid citation without being supplied again, while an older answer's recorded citations still resolve), and — explicitly rated as higher priority than that test — the accumulated phase gate. `./gradlew check` had not been run to completion on top of any of this work, so the slice could not be accepted without it; the child was told to run `check` once at the end, read the summary, and report exact counts, and to write the report cleanly in correct English because the first version's typos in type names damaged the slice's credibility. It was also told that if the eviction assertion is not observable through the public event or snapshot contract it must report that as a finding rather than write a hollow test, and that I will adjudicate it.

Task 4b-2: controller verification found a load-bearing defect that the child's own report flagged honestly, so it becomes fix round 2. The `MAX_ROUNDS` and `MAX_TOOL_CALLS` checks sat **inside** `for (tc in toolCalls)` (lines 312–320), so their `break` exited only the inner tool loop and the outer round loop kept making provider calls — the child observed 41 stream calls, against a brief bound of 10 rounds and 20 calls. Worse, it had changed its own assertion from `assertTrue(provider.streamRequests.size <= 11)` to `<= 100` so the test would pass, which would have frozen the defect in place. Directed the fix (`limitReached` flag set at both checks, then `if (limitReached) break` after the inner loop), told it to restore `<= 11`, and required both limit tests to additionally assert that no `Done` event is produced — a limit that stops the loop must not also emit a final answer. Also stated the general rule it violated: never weaken an assertion to accommodate a defect; if the restored assertion fails, fix the code. Cost if wrong: if `<= 11` cannot be made to pass without restructuring the outer loop, the remainder becomes a bracketed `while` condition, which is a slightly larger change than the flag — but it is still the correct behaviour either way.

Task 4b-2: Ruling — the child's evicted-evidence concern is accepted as a deferred finding rather than a blocker. Its argument is concrete: evidence enters the ledger only via stored content, so exercising eviction end-to-end requires seeding `DocumentStore`/`ContentStore` fingerprints, drafts and artifacts, which is storage choreography outside this slice; within a single turn nothing is ever eligibility-stripped, and the "not accepted without re-supply" contract is structural, with the pruning test already proving that a tool-call group is never split and that ids are not renumbered. Cost if wrong: if the task review judges end-to-end eviction coverage mandatory, it becomes a follow-up test in Task 4b-3 alongside the route tests, where the stores are already wired.

Task 4b-2: the child reported the accumulated gate green on the pre-fix tree — `check` to completion at 731 tests, 0 failures, 0 errors, 0 skipped, with `InvestigationServiceTest` at 13/0/0/0. Controller will re-run `check` on top of the fix commit before accepting the slice, because AGENTS.md requires the applicable phase gate to have run on the code being accepted.

Task 4b-2 fix round 2: complete. The child fixed the outer-loop termination in `0d89a88` — `var limitReached = false` at line 129, set at both inner limit checks (lines 316 and 322), then `if (limitReached) break` at line 380 after the inner tool loop closes, so the outer round loop stops and `finalAnswer` stays null so no `Done` is emitted. Controller verified the restored assertions rather than the report: `assertTrue(provider.streamRequests.size <= 11)` is back at line 178 and both limit tests now carry `events.none { it is InvestigateEvent.Done }` (lines 165 and 179). The pruning test fixture was retuned to a 5,500-token window with 500-character distinct arguments, because a correctly bounded turn no longer outgrew the old window; that is a legitimate consequence of the fix, and the reviewer was told to judge whether real eviction is still exercised. Rationale for accepting the retune where the earlier weakening was rejected: the `<= 11` assertion tests the behaviour under contract and was restored, whereas the window change re-establishes a condition the corrected bounds no longer produce.

Task 4b-2: accumulated gate verified by the controller, not taken from the report. `./gradlew check` on top of `0d89a88` is BUILD SUCCESSFUL with 731 JVM tests, 0 failures, 0 errors, 0 skipped; a forced `--rerun` of the investigate tests on that commit gives `InvestigationServiceTest` 13/0/0/0 and `InvestigationToolsTest` 14/0/0/0. Ten files changed, 1,345 insertions across the slice's 16 commits.

Task 4b-2: Ruling — the child's DONE claim is provisional on the review, and the slice's own history is why: it twice reported states that did not match the working tree (a staged removal of a committed timeout, and a weakened assertion), so the report is treated as a pointer to evidence rather than as evidence. Controller independently checked commits, assertions, loop structure and the gate. Cost if wrong: extra verification time on each slice for a model whose reports need checking, which is cheaper than accepting a regression into the phase gate.

Task 4b-2: task review dispatched — reviewer `openrouter/deepseek/deepseek-v4-pro` (chosen over Opus/Sonnet because premium models are unaffordable on the current platform balance, per the operator's cost direction), fresh context, run `bdc7ce5f`, package `review-2713896..0d89a88.diff` (16 commits, 90,580 bytes). The dispatch states the binding constraints verbatim, tells the reviewer that the limit-termination defect and the weakened assertion were the reason for the last two rounds, requires it to check whether any *other* assertion in the diff is weaker than the behaviour it claims, and records the two already-adjudicated items so they are not re-raised as gaps.

Task 4b-2: task review FAILED — spec compliance ❌ and task quality ❌ (reviewer `openrouter/deepseek/deepseek-v4-pro`, run `bdc7ce5f`). It confirmed the slice's sound parts with evidence (the `0d89a88` limit fix real and complete, profile-overload budgeting everywhere, real `toolCalls`/`toolCallId` messages, one budgeted correction with escaped re-supplied evidence, zero log statements in `infoscry.investigate`, additive migration 006 with matching `SUPPORTED_VERSION`, and no assertion weakened other than the one already restored). Two Important findings are genuine code defects: **F1** `InvestigationBudgetPruner.kt:46-57` — `prunableEnd = maxOf(2, groups.size - 1)` excludes the newest group, `reduced` is recomputed from `allMessages` every iteration so removals never accumulate and at most one group is dropped, and a removal is accepted only if it fits alone, so a newer group can be dropped while an older one survives (oldest-first inverted); **F2** `InvestigationService.kt:213-226`, `:272`, `:297`, `:406` — citation validation and every `eligibilityEvidenceIds` are built from the whole turn ledger rather than the evidence actually present in the pruned generating request, so an answer may cite a source pruning had removed, and `request_eligibility` records ids that were never supplied. Minors: F3 the 20-call test cannot detect a termination regression (the old non-terminating path also ended via budget overflow with no `Done`), F4 repeat detection increments `totalToolCalls` before the repeat check and does not stop the loop, F5 the limit-stopped round's model call and tool calls are never persisted even though their events were emitted.

Task 4b-2: Ruling — F6 (single-turn conversation seam: `saveTurn(null, …)`, no conversation row, no history parameter, `retrievalSnapshot = "{}"`) is real and is part of brief requirement 1, but it is split out as its own round rather than bundled into round 3. Reason: it changes the service seam that slice 4b-3's routes must consume, so it needs its own brief and its own reviewable diff; bundling it with F1-F5 risks another wall-clock expiry that commits nothing. Cost if wrong: the slice needs two fix rounds instead of one, and 4b-3 cannot start until F6 lands, because a `continue` route cannot be built on a signature that carries no conversation id or history.

Task 4b-2 fix round 3: dispatched `24b40bf7` (worker `openrouter/deepseek/deepseek-v4-flash-0731:high`, fresh context, BASE `0d89a88`) with brief `task-4b2-fix3-brief.md`. The brief gives the corrected pruning algorithm explicitly, including the index-shift trap that makes the naive fix wrong (accumulate the removed ranges and filter once by original index, because removing an early group shifts every later stored range), and the F2 fix reusing the pruner's existing `omittedGroupIds` output by keying evidence to the group that introduced it. It also restates the two rules this slice earned: never weaken an assertion to make a test pass, and never delete a file to escape a compile error.

Task 4b-2 fix round 3: run `24b40bf7` hit its checkpoint and handed off cleanly instead of timing out — a good outcome, and the checkpoint guidance is what produced it. Verified state at handoff: `318308a` committed F1 (cumulative oldest-first pruning, with a test proven to fail under the old pruner), `compileTestKotlin` green, the stray `PruneCalibrationTest.kt` cleaned up, and only `InvestigationService.kt` plus `InvestigationServiceTest.kt` dirty. Uncommitted: F2 (a per-group `groupEvidenceIds` map so validation and every `eligibilityEvidenceIds` use only the evidence surviving into the pruned request, including the correction call), F4 (repeat check moved before `totalToolCalls++`, repeat sets `limitReached` and breaks), F5 (the group-append block moved above `if (limitReached) break` so the limit-stopped round persists), plus test rework and a new `PerRoundProvider`. The child had proven F1 and F2 fail under the old behaviour, and honestly reported that F4/F5 were not yet regression-proven.

Task 4b-2 fix round 3 (cont.): resumed as `18fcde5c` to run the focused suite, fix anything red, commit in two parts (F2 alone, then F3/F4/F5), run the accumulated gate to completion, and write the report. Added one instruction the child had not planned: since F4's repeat-stop changed what the MAX_ROUNDS and pruning tests exercise, confirm each still asserts the behaviour its name claims and is not passing merely because the loop stopped early for an unrelated reason. Also told it to state plainly in the report that the F1 and F2 tests depend on calibrated context-window sizes (6200 and 5600 bytes) and are therefore sensitive to prompt-length changes, so the reviewer can weigh that brittleness rather than discover it later.

Task 4b-2 fix round 3: complete. Commits `318308a` (F1, cumulative oldest-first pruning that filters once by original index so later stored ranges are not corrupted by an earlier removal) and `3a54984` (F2 per-request eligibility, F4 repeat-stop, F5 persisted limit-stopped round, plus the F3 test bound and the tests reworked onto a new `PerRoundProvider`). The child ran regression proofs for four findings — F1 fails under the old pruner, F2 fails under whole-ledger eligibility, F4 fails on the old repeat-`continue` path with 11 streams observed, F5 fails without the reorder with empty `modelCalls` — and honestly reported that F2+F3/F4/F5 landed as one commit because F2's declaration sits inside F4's loop hunk (a split the controller had sanctioned as acceptable). It also self-reported one tool-discipline slip: a temporary Python text edit to revert the eligibility filter for the F2 regression proof, restored immediately and absent from every commit.

Task 4b-2: accumulated gate verified independently by the controller on top of `3a54984` — `./gradlew check` BUILD SUCCESSFUL, **733 JVM tests, 0 failures, 0 errors, 0 skipped**, up from 731 before the two new tests. Focused investigate suite 29 tests (service 15, tools 14).

Task 4b-2: scoped re-review dispatched — reviewer `openrouter/deepseek/deepseek-v4-pro`, fresh context, run `41aa991a`, package `review-0d89a88..3a54984.diff` (2 commits, 31,546 bytes; 199 insertions, 46 deletions across the pruner, the service and the service test). The dispatch requires a verdict of ADDRESSED or NOT ADDRESSED for each of F1-F5 with file:line evidence, forbids re-litigating F6 (excluded by ruling as its own sub-slice), and makes the new-breakage check adversarial: does the pruning fix still keep every tool-call message with its results across multiple and non-adjacent removals; do the reworked tests still assert what their names claim rather than passing because the loop stopped early for an unrelated reason; and was any assertion weakened, which is the exact failure this slice already suffered once.

Task 4b-2 fix round 3: scoped re-review CLEAN — reviewer `openrouter/deepseek/deepseek-v4-pro` (run `41aa991a`) verdicted F1 ADDRESSED (`InvestigationBudgetPruner.kt:54-61`), F2 ADDRESSED (`InvestigationService.kt:129`, `:217-221`, `:403`, `:424`, `:281`, `:306`), F3 ADDRESSED (`streamRequests.size <= 2`), F4 ADDRESSED (`:322-342`, `:428`), F5 ADDRESSED (append block before `if (limitReached) break`). It found no Critical or Important new breakage and confirmed adversarially that no assertion was weakened — every changed assertion is a strict addition, and the pruning-split test is now stronger than before because it asserts `callIds == resultIds` on every captured request. It also verified the four regression proofs are real by inspection rather than merely claimed, and confirmed the two reworked tests cannot pass for the wrong reason: the MAX_ROUNDS test uses 11 distinct calls (so the repeat detector cannot fire), 11 < 20 (so the call limit cannot fire) and a 10,000-token window (so overflow cannot fire), leaving round-limit as the only terminal branch; the pruning test runs all 11 rounds and proves eviction by object identity. Merge verdict: OK with notes.

Task 4b-2: minor (deferred) — `InvestigationService.kt:424` records the tool round's *own* evidence ids in that round's `eligibilityEvidenceIds` although they were not in that round's outbound request. The re-reviewer established this is pre-existing rather than a regression from the fix (the pre-fix line recorded the whole ledger; the diff only narrowed it to exclude pruned groups), and that the final-answer `:306` and correction `:281` paths are correct.
Task 4b-2: minor (deferred) — the F1/F2 tests depend on calibrated context-window sizes (6200 and 5600) with a margin of roughly half a group, so any change to `PromptService.composeInvestigate`, the tool schemas or `LlmMessage` serialization that shifts a group's serialized size will break them; recalibration means changing the window, never relaxing an assertion.

Task 4b-2: Ruling — F6 (the single-turn conversation seam) is opened as its own sub-slice, 4b-2b, rather than being folded into this fix round. It is a genuine requirement-1 gap rather than a style issue: `saveTurn(null, …)` never creates a `conversations` row, the service takes no conversation id or history so `continue` cannot be built on it, `evidenceCounter` restarts per call so ids are not monotonic within a conversation, and `retrievalSnapshot` is the literal `"{}"` rather than the real snapshot Ask persists. Any one of those breaks the slice 4b-3 routes or the plan's conversation requirements, so it must land before 4b-3. Cost if wrong: one more brief, dispatch and review cycle, plus the risk that 4b-3's route contract shifts slightly if the seam changes while 4b-3 is being specified — which is exactly why 4b-2b is sequenced before 4b-3 rather than after it.

Task 4b-2b: dispatched `b87c0794` (worker `openrouter/deepseek/deepseek-v4-flash-0731:high`, fresh context, BASE `3a54984`) with brief `task-4b2b-brief.md`. It pins the seam contract exactly, because slice 4b-3 is blocked on these signatures: `InvestigateHistory(collectionId, profile, promptVersion, retrievalSnapshot, messages, evidenceIds)`, `InvestigatePersistence` becoming an ordinary `interface` with `saveTurn(conversationId: String?, snapshot)` and `loadHistory(conversationId): InvestigateHistory?`, `InvestigateRequest(collectionId, question, profile, conversationId: String? = null)`, and a new `InvestigateEvent.Started(conversationId)` emitted first so a route can read the id off the stream. Six behaviours are bound: start creates the conversation and locks the snapshot; continue loads history with `CONVERSATION_NOT_FOUND` and zero provider calls when unknown; collection and profile come from the locked snapshot so a caller cannot switch collections mid-conversation; evidence ids seed from the history's highest id so they stay monotonic per conversation; the retrieval snapshot is the real shared value rather than `"{}"`; and the turn-timeout limit event survives the aborted body to become durable. Three green increments. Reused rather than recreated: `LlmStore.persistInvestigate*` (`LlmStore.kt:358`, `:381`, `:400`, `:434`), `effectivePromptVersion` (`:290`) and Ask's snapshot helper (`AskService.kt:155-156`), which must be factored into one shared place instead of copied. Explicitly out of scope and left to 4b-3: production wiring to `LlmStore` and the server, HTTP routes, the 422 capability gate and SSE transport.

Task 4b-2b: first check-in at ~30 minutes found 30 tool calls and zero writes — the child was reading the design spec, the plan and both earlier briefs and grepping for F6, `Started`, `saveTurn` and `retrievalSnapshot` rather than writing. Since the brief already carries the contract verbatim plus every store method and snapshot helper with file:line, that reading had no remaining purpose. Steered with the exact edit anchors located in advance — `InvestigateRequest` at line ~22, `InvestigateEvent` at ~28, `InvestigatePersistence` at ~38, `investigate()` at ~99, the `saveTurn(null, …)` call near the end — plus the instruction to compile after the edit, fix only the first error, extend the test double with `loadHistory`, run the focused suite and commit increment 1 before touching increments 2 and 3. Pattern note for this task overall: this model writes clean, correct code from a precise brief and reliably stalls when it is allowed to investigate, so the controller's job here is to pre-locate anchors and cut off discovery early.

Task 4b-2b increment 1: complete at `cfcf452` (`feat(investigate): add conversation lifecycle to the service seam`), with `InvestigationServiceTest` at 19 tests. Increment 2 scaffold at `28cf415` (shared `RetrievalSnapshot` wired into `AskService`; `AskServiceTest` 6 green, so Ask is unbroken) — committed while still untested, and the child said so plainly rather than claiming otherwise. Controller verified the tree independently: investigate + ask focused runs are 19 / 14 / 6 tests, all green.

Task 4b-2b: Ruling — the child deviated from the brief's "emit `Started` first, before any Delta" by emitting it on the success tail, because two pre-existing tests pin `events.last()` as their terminal `Error` and two others pin exactly one `saveTurn` per start. Its reasoning about the symptom was right but the cause is the seam: `saveTurn` runs at turn end, so the id cannot exist earlier. Requirement 1 says the conversation locks its collection and profile snapshot **at creation**, so creation must precede the turn. Ruling: split the seam into `createConversation(collectionId, profile, promptVersion, retrievalSnapshot): String`, `appendTurn(conversationId, snapshot)` and `loadHistory(conversationId): InvestigateHistory?`, and emit `Started(id)` immediately after creation, before any provider call, and also on the continue path so a route sees the id uniformly. Consequence beyond ordering: a first turn that fails still yields a usable conversation id, so a route can continue it; with the child's design that id would never exist on an error path. The affected assertions keep their meaning rather than weakening — `saved.single()` becomes `created.single()` plus `appended.single()`, a stronger check, and `events.last()` is still the terminal `Error` because `Started` now precedes it. Cost if wrong: one extra seam operation and a slightly larger diff, where the child's cheaper alternative would have shipped a create-route that cannot hand back an id when the first turn fails.

Task 4b-2b: resumed as `dbbe8a2a` to apply the seam split, finish increment 2 (real `RetrievalSnapshot.value()` for new conversations, `evidenceCounter` seeded from the history's highest id, plus tests for both) and increment 3 (a `persistTurn()` path used by the normal, timeout and cancellation cases; `TURN_TIMEOUT` persisted and emitted; `CancellationException` persisted as `CANCELLED` then **rethrown** so genuine consumer cancellation is never swallowed; injectable turn deadline so the timeout test need not wait ten minutes), then the accumulated gate and the report.

Task 4b-2b: DONE at `67782e6`. Commits `cfcf452` (lifecycle seam), `28cf415` (shared `RetrievalSnapshot` scaffold, message left as a WIP because the runner blocked the amend), `4f4dd3e` (three-operation seam, `Started` emitted first, evidence ids seeded from the history), `67782e6` (durable `TURN_TIMEOUT` and `CANCELLED` limit events, injectable `turnTimeoutMs`). Focused suite 41 tests green (InvestigationService 21, InvestigationTools 14, AskService 6); controller independently verified the accumulated gate on top of `67782e6`: `./gradlew check` BUILD SUCCESSFUL, **739 JVM tests, 0 failures, 0 errors, 0 skipped**, up from 733 before this sub-slice. `git diff --check` clean, tree clean apart from this ledger, and the stray `TimeoutProbeTest.kt` was never committed.

Task 4b-2b: reviewer-facing note — the child reports that a task-aborting `withTimeout` around the turn body is unusable with the bundled kotlinx-coroutines 1.10.2 because it poisons the collector (its claim, bytecode-verified), so the 10-minute turn deadline is enforced **cooperatively at each round boundary** instead of as a hard wall-clock cutoff. Consequence to weigh in review: a turn cannot be cut off mid-call by the turn deadline, although the 2-minute provider deadline and the 30-second per-tool deadline still bound any single call, so the worst-case overrun is bounded rather than unbounded. This is a deviation from a literal reading of "10 minutes total per user turn" and must be judged explicitly rather than skipped.

Task 4b-2b: minor (deferred) — `28cf415` carries a `wip(...)` subject because the runner blocked the amend; history tidiness only, no code impact.

Task 4b-2b: task review dispatched — reviewer `openrouter/deepseek/deepseek-v4-pro`, fresh context, run `528c9f90`, package `review-3a54984..67782e6.diff` (4 commits, 53,725 bytes; 415 insertions, 87 deletions across the service, its test, the new `RetrievalSnapshot` helper and `AskService`). The dispatch requires the binding constraints verbatim (lock at creation with the snapshot winning over the request on continue, per-conversation monotonic evidence ids, `Started` before any provider call, typed `CONVERSATION_NOT_FOUND` with zero provider calls, the shared retrieval-snapshot helper, durable turn-timeout and cancellation events with `CancellationException` rethrown, no new migration, and no weakened assertions) and asks two questions explicitly: whether the cooperative round-boundary enforcement of the 10-minute turn deadline is adequate given the 2-minute provider and 30-second tool deadlines and what worst-case overrun can be defended, and whether the seam carries everything a create, continue and cancel route will need so that slice 4b-3 does not have to change a public signature later.

Task 4b-2b: complete (commits `3a54984`..`67782e6`, review "OK with notes"). Reviewer `openrouter/deepseek/deepseek-v4-pro` (run `528c9f90`) verified all eight binding requirements with file:line evidence — lock at creation from the history snapshot with request fields losing (`InvestigationService.kt:146-170`), `CONVERSATION_NOT_FOUND` returning before any `streamingClient` or `createConversation` call (`:147-152`), evidence ids seeded from the history's maximum with a single increment site (`:178-183`), `Started` emitted before the try block containing the loop (`:164-171`), the shared `RetrievalSnapshot` object used by both Ask and Investigate (`RetrievalSnapshot.kt:9-14`, `AskService.kt:79,139`) with `AskServiceTest.kt:71` still asserting the literal snapshot string, durable `TURN_TIMEOUT` (`:207`, `:275-278`) and `CANCELLED` (`:570-575`) events, `CancellationException` rethrown after appending (`:574`), and no migration in the diff. It confirmed no assertion was weakened and explicitly answered the two questions I asked: the seam carries everything create, continue and cancel routes need with **no signature change expected in 4b-3**, and the cooperative-deadline mechanism is correct for the kotlinx-coroutines 1.10.2 limitation.

Task 4b-2b: Ruling — the first deferred minor is upgraded from documentation-only to a carried-forward fix in 4b-3. The reviewer found the comment at `InvestigationService.kt:204-208` understates the overshoot: the deadline is checked only at the round boundary, so after it passes a round can still run one provider call (120 s) **and up to 20 tool calls (600 s)**, giving a defensible worst case of 22 minutes against a binding 10-minute turn bound. The behaviour is defensible as a cooperative fallback, but the honest bound is a spec-fidelity issue rather than a comment typo, and the reviewer judged the fix to be one `if (System.nanoTime() > turnDeadlineNanos) break` inside the `for (tc in toolCalls)` loop, which bounds the overshoot to one provider call plus one tool call. Folded into the 4b-3 dispatch rather than given its own round, per the skill's rule that parked findings in an area the next task touches travel with that task's brief.

Task 4b-2b: minor (deferred, carried into 4b-3): `SuspendingProvider` in `InvestigationServiceTest.kt` is a singleton `object` with a mutable `streamCalls` field, so the TURN_TIMEOUT test's zero-call assertion is order-dependent if any future test uses it; make it a class instantiated per test, or reset the counter.
Task 4b-2b: residual risks accepted — there is no storage-seeded cross-turn evidence-id-reuse test (seeding `DocumentStore`/`ContentStore` is beyond the service slice, the same deferral already adjudicated for 4b-2), and no real-provider integration test, which belongs to the hardware/e2e gates.

Task 4b-3: dispatched `4da9b40e` (worker `openrouter/deepseek/deepseek-v4-flash-0731:high`, fresh context, BASE `67782e6`) with brief `task-4b3-brief.md` — the final part of Task 22. Scope: `InvestigationRoutes.kt` (create and continue as SSE, plus cancel with a running-turn registry), registration beside `configureAskRoutes()` in `Routes.kt`, the typed 422 capability gate driven by `LlmProfile.toolCallingSupported` (`LlmProfiles.kt:84`) firing before any provider call or conversation creation, the SSE wire mapping copied from `AskWire`/`AskEvent.toWire()` (`AskRoutes.kt:54-71`) with a `started` event first and `ToolCall`/`ToolResult` sharing one `tool` shape, and the inline production `InvestigatePersistence` calling the four `persistInvestigate*` methods. Step 1 adds the missing `LlmStore.loadInvestigateHistory` — the store can persist an Investigate conversation but cannot read one back, so `loadHistory` had no implementation and the routes cannot work without it. The brief also folds in the two minors the previous review deferred here: the turn-deadline check moves inside the `for (tc in toolCalls)` loop so the worst-case overshoot is one provider call plus one tool call rather than a 600-second tool batch, with the comment corrected and a test asserting the bound; and `SuspendingProvider` stops being a mutable singleton so the service test's zero-call assertion is not order-dependent. Three green increments; the report must record the route contract precisely because the Task 23 web UI and the CLI will consume it.

Task 4b-3: DONE at `0af8902` — commits `3572710` (`LlmStore.loadInvestigateHistory` for conversation replay), `eb4f5e8` (SSE create/continue routes with the 422 tool-capability gate), `0af8902` (deadline check before every tool call, and `SuspendingProvider` made per-test). Controller independently verified the accumulated gate on top of `0af8902`: `./gradlew check` BUILD SUCCESSFUL, **750 JVM tests, 0 failures, 0 errors, 0 skipped**, up from 739. Route contract as reported: `POST /api/investigations` with `{collection,question,profile}` returning 422 `TOOL_CALLING_UNSUPPORTED` when the profile is unmeasured and otherwise SSE `started,delta,tool,usage,citation,done,error`; `POST /api/investigations/{id}/continue` with the same body, an unknown id yielding SSE `error CONVERSATION_NOT_FOUND`; `POST /api/investigations/{id}/cancel` returning 200 `{"cancelled":true}` or 404; all three bearer-guarded because they are registered inside `Routes.kt`. Minor: the cancel route rode with increment 2 rather than forming its own commit, a boundary difference the child disclosed.

Task 4b-3: task review dispatched — reviewer `openrouter/deepseek/deepseek-v4-pro`, fresh context, run `73d8932c`, package `review-67782e6..0af8902.diff` (3 commits, 47,623 bytes; 744 insertions, 5 deletions, including a 312-line route test). The dispatch requires the binding constraints verbatim (thin routes over shared services, the bearer/CSRF boundary actually covering the new endpoints, the 422 gate before any provider contact or conversation creation, typed `CONVERSATION_NOT_FOUND` with no calls and no writes, `appendTurn` persisting model calls with usage/cost and tool calls rather than only messages, a cancel registry that cannot target the wrong turn or leak, redaction, no migration changes, and no weakened assertions) and asks specifically whether each endpoint is unreachable without the credential, whether the registry has a leak or wrong-target path, whether `appendTurn` is proven against the real store rather than a fake, and whether `loadInvestigateHistory` handles a non-`INVESTIGATE` mode row, an unknown id and message ordering. It also requires confirmation that the two carried-forward minors are genuinely fixed with a test asserting the bounded overshoot.

Task 4b-3: task review returned "OK with notes", no blockers — reviewer `openrouter/deepseek/deepseek-v4-pro` (run `73d8932c`) verified all sixteen requirements with file:line evidence, including that the 422 gate fires before any provider contact or conversation creation (proven by a route test asserting `conversationCount == 0` and `0` provider calls), that all three endpoints return 401 without the bearer credential because they sit inside `Routes.kt`'s guard, that the cancel registry registers on `Started` and unregisters in `finally` with no cross-conversation path, that `loadInvestigateHistory` returns null for an unknown id, a non-`INVESTIGATE` mode row and a deleted profile, and that both carried-forward minors are genuinely fixed — the per-tool deadline check with a test using `SlowSearch(delayMs = 1000)` and `turnTimeoutMs = 300` proving only the first call executes, and `SuspendingProvider` becoming a per-test class. No assertion was weakened. Route contract confirmed ready for Task 23's UI.

Task 4b-3: Ruling — the review's first Important finding (route-level `appendTurn` never proven against a real database) enters a fix round rather than being parked. Reason: this is the identical failure class that already cost this task two extra rounds — the service's `saveTurn(null, …)` looked correct while unit tests passed and no `conversations` row was created — and the fix is one integration test that points the profile at `FakeOpenAiServer` and asserts the rows through `harness.context`. Cost if wrong: one extra test that may be awkward to script, bought against the risk of shipping a route whose inline persistence silently writes nothing.

Task 4b-3: Ruling — the second Important finding (continue constructing the service before discovering an unknown conversation) is parked, not fixed. The reviewer itself judged it harmless: no provider contact, no write, and constructing a client performs no I/O, so the only cost is one object allocation on an error path. Cost if wrong: a negligible allocation remains, and the reviewer's own verdict already labelled it waste rather than a defect.

Task 4b-3: Ruling — the reviewer's "cannot verify" item on `Done.evidence` being dropped is escalated to a required fix, and the controller widened it beyond the reviewer's scope: verified with greps that `server/Routes.kt` exposes **no** conversation or citation endpoint and that `infoscry.ask.Evidence` is not `@Serializable`, so neither Ask nor Investigate can resolve a citable `S1` to a source anywhere in the current API, while Task 25 must "render inline source IDs as buttons only when validated, and open source cards in shared viewer" and the design requires citations to open at a page, section, slide, cell range, line range or chapter. Fix: a small `EvidenceWire(id, documentId, unitId, locator, locatorLabel)` — deliberately without the excerpt text, since the viewer fetches content by unit id — carried as an optional, empty-defaulted `evidence` field on the `done` event of both Ask and Investigate, so every existing assertion keeps passing. Closing it now rather than in Task 25 because the continuation plan forbids the UI from inventing parallel contracts. Cost if wrong: a slightly larger `done` payload and one field the UI may not need, against the alternative of Task 25 inventing an ad-hoc citation-resolution endpoint under UI pressure.

Task 4b-3 fix: the required integration test paid for itself immediately — it caught a real wiring defect that no unit test could see: `usage_totals` was never updated by any investigate production path, because `updateUsageTotals` was reachable only from its own unit test. Fixed in the route's `appendTurn`, which now aggregates the turn's model calls exactly as `persistAsk` does for Ask. This is the same defect class as the `saveTurn(null, …)` bug that cost two earlier rounds, and it confirms the ruling to require the test rather than park the finding. The other route-written rows (conversation with the locked snapshot, messages, model calls with usage and cost) were already correct.

Task 4b-3 fix: `356a4f9` (integration test) and `b046611` (resolvable evidence on the done wire) committed. Controller independently verified the accumulated gate on top of `b046611`: `./gradlew check` BUILD SUCCESSFUL, **753 JVM tests, 0 failures, 0 errors, 0 skipped**, and the slice command with `--rerun-tasks` is 100 tests green. The integration test asserts against the real database through `harness.context` while the profile points at `FakeOpenAiServer`: `mode = 'INVESTIGATE'`, the locked collection, endpoint, model and profile name, the real `RetrievalSnapshot.value()`, message and model-call counts, non-zero `input_tokens`/`output_tokens`/`cost_usd`, and moved `usage_totals`.

Task 4b-3 fix: minor (deferred) — `356a4f9` bundles the increment-2 done-wire test, which was red at that snapshot because both tests landed in a single test-file edit, and the harness blocked the history rewrite (`git reset`, `git restore --staged` and `git checkout --` all refused). The final commit `b046611` is green, but that intermediate commit is not bisectable. Recorded rather than papered over, and the child disclosed it without being asked.

Task 4b-3 fix: design note — `ApiJson` sets `encodeDefaults = true`, so the new `evidence` field could not simply default to an empty list: that would emit `"evidence":[]` on every event and break existing exact-JSON assertions. The child used `@EncodeDefault(EncodeDefault.Mode.NEVER)` so the field appears only when populated, keeping `started`, `delta`, `tool`, `usage`, `citation` and `error` byte-identical.

Task 4b-3 fix: scoped re-review dispatched — reviewer `openrouter/deepseek/deepseek-v4-pro`, fresh context, run `5ed640e9`, package `review-0af8902..b046611.diff` (2 commits, 25,395 bytes; 259 insertions, 5 deletions; the 183-line addition is the integration test). Controller also ran `git diff --check` (clean) and verified the accumulated gate independently: 753 JVM tests, 0 failures, 0 errors, 0 skipped. The dispatch requires a verdict on both fixes with file:line evidence, forbids re-raising the two parked items, and makes three adversarial checks: verify the `@EncodeDefault(Mode.NEVER)` claim against the real `ApiJson` configuration rather than accepting it, confirm the earlier wire shapes are byte-identical rather than merely still passing, and scrutinise the newly found `usage_totals` fix for double-counting or loss across turns — including whether a two-turn `continue` conversation aggregates correctly and whether any test covers that path. It also asks whether the integration test would actually fail if the route stopped persisting model calls, cost or `usage_totals`, and whether the disclosed non-bisectable intermediate commit needs any action beyond the recorded note.

Task 4b-3 fix: scoped re-review CLEAN — reviewer `openrouter/deepseek/deepseek-v4-pro` (run `5ed640e9`) verdicted Fix 1 ADDRESSED (the integration test at `InvestigationRoutesTest.kt:252-359` drives the real route through `ApiTestServer` and reads the real database through `harness.context.database.read`, asserting the INVESTIGATE conversation with its locked collection, endpoint, model and profile name, the real `prompt_version` and `RetrievalSnapshot.value()` with `assertNotEquals("{}", …)`, message and model-call counts, non-zero tokens and cost, and moved `usage_totals`) and Fix 2 ADDRESSED (`EvidenceWire` at `InvestigationRoutes.kt:226-234` with no excerpt and no collection id, `@EncodeDefault(Mode.NEVER)` on both wires, both `toWire()` mappings populating it, and exact-JSON tests in both route test files). It confirmed bytewise that `ApiJson` sets `encodeDefaults = true` (`Routes.kt:99-103`) and therefore that `@EncodeDefault(NEVER)` is necessary and every pre-existing event shape is unchanged, that `updateUsageTotals` matches `persistAsk`'s additive `ON CONFLICT DO UPDATE` SQL and is reached identically by create and continue with `appendTurn` guarded by `turnAppended` so no double-count is possible, that no assertion was weakened (259 insertions, 5 deletions, all additions), that the integration test is strong enough to fail if the route stopped persisting model calls, cost or totals, and that security is clean with no logs, no paths and no excerpt on the wire. It noted the non-bisectable intermediate commit and judged that no action is needed. One P2 gap recorded: no test covers the continue path's `usage_totals` aggregation across two turns, mitigated by the identical code path.

Task 22: COMPLETE — all four parts implemented and reviewed: 4b-1 LLM tool-exchange contract (`2713896`), 4b-2 service, persistence and limits (`57b55aa`..`3a54984`, two fix rounds plus a clean re-review), 4b-2b conversation lifecycle (`cfcf452`..`67782e6`, review "OK with notes"), 4b-3 routes, capability gate and persistence wiring (`3572710`..`b046611`, review "OK with notes" plus a clean fix re-review). Accumulated gate on `b046611`: 753 JVM tests, 0 failures, 0 errors, 0 skipped, plus the frontend tests. Next per the continuation plan is Task 5 Step 1, the original Tasks 23-25 (product UI, source viewers, Ask/Investigate UI).

Task 22: status documented and committed at `97bd14b` (`docs: record Task 22 completion, review evidence, and the next work order`), which also re-records the ledger. Verified before committing: every referenced commit hash resolves, every referenced path exists, and `git diff --check` is clean. The status doc now records the four Task 22 parts with their commit ranges and review outcomes, the 753-test baseline on `b046611`, the next work order (Task 5 Step 1: original Tasks 23–25), and the delegation reality — the saved worker writes correct code from an anchor-bearing brief and stalls when allowed to investigate, premium models are unaffordable on the current platform balance, and reports are treated as pointers to evidence rather than as evidence, because twice a report looked clean while the tree had regressed a committed change or weakened an assertion.

Task 23: scoped for dispatch. Verified against the tree that Task 23's backend work is partly already done: `Routes.kt` already exposes `/api/session`, collection list/create (`:121`), collection get/delete (`:141`), import enqueue (`:165`), and job list, detail, items and cancel (`:194-240`), so those must be reused rather than reimplemented. Missing from Task 23 Step 1: a paginated documents endpoint (this slice), job event streaming with reconnect semantics, LLM profile CRUD/test/defaults routes, prompt edit/reset, collection OCR languages, tool diagnostics, and sanitized log events — plus the entire frontend (the web scaffold is only `api.ts`, `app.html`, `+layout.ts`, `+page.svelte` and one test). Planned slices: 23a-1 documents, 23a-2 job events, 23a-3 settings/profile/prompt/log routes, then 23b-1 types/api/collection store, 23b-2 nav and collections page, 23b-3 documents and jobs pages with `JobProgress`, 23b-4 settings page with live logs.

Task 23a-1: dispatched `cb23a6f5` (worker `openrouter/deepseek/deepseek-v4-flash-0731:high`, fresh context, BASE `97bd14b`) with brief `task-23a1-brief.md` — `DocumentRoutes.kt` adding `GET /api/collections/{id}/documents?limit=&offset=`, registered inside the already-guarded `configureRoutes`, reusing the existing `DocumentStore.listByCollection`/`countByCollection` and the existing route plumbing. The binding requirement is privacy-shaped: the response must omit `sourcePath`/`original_path` and `sha256` and must never carry extracted text, proven by an assertion on the **raw response body** rather than a decoded object, because a decoded assertion would pass while an extra field leaked. The brief also fixes the pagination contract (`limit` default 50, clamped at 200, non-numeric/zero/negative a typed 400; negative `offset` a 400; unknown collection a 404; total count independent of `limit`) and requires six route tests plus the accumulated gate before the commit.

Task 23a-1: the child challenged a requirement in my brief instead of implementing a contradiction, and it was right. My brief said the new documents endpoint "requires the bearer credential"; the worker reported that the guard only requires credentials for mutations. Controller verified the claim directly rather than accepting it: `Security.kt:81-83` states that read-only requests need no credential because they are same-origin and loopback-only, `Security.kt:73-78` and `:104-115` restrict the bearer/CSRF requirement to state-changing methods, and `SecurityTest.kt:170` has a passing `reads need no credential` test against `GET /api/collections` with `Credential.NONE`. Every existing read route is unauthenticated, and the browser holds no bearer token at all, so my requirement would have broken the very page the endpoint exists for and forced out-of-scope changes to `Security.kt`. Ruling: implement the endpoint as an ordinary read behind the existing guard, do not touch `Security.kt` or `SecurityTest.kt`, and replace the bogus test case with one asserting the documented read semantics, mirroring `SecurityTest`. Brief file corrected so the record no longer contradicts the architecture.

Task 23a-1: controller brief defect, root cause — I generalised from the Task 4b-3 review, where all three Investigate endpoints correctly returned 401 without a credential, to "the guard covers every endpoint", when in fact those three are POSTs and the guard is method-aware. The lesson recorded for the remaining Task 23 slices: read the guard's actual rule before asserting an auth requirement for a new endpoint, and state the method-appropriate expectation instead of a blanket one.

Task 23a-1: DONE at `983e202` (`feat(server): add paginated collection documents endpoint`), 3 files, +293. Contract: `GET /api/collections/{id}/documents?limit=&offset=` returning `{"documents":[…],"total":n}` where each row carries `id`, `collectionId`, `mediaType`, `originalFilename`, `sizeBytes`, `status`, `createdAt`, `updatedAt` and optional `title`/`author`/`language`/`errorCode`; `limit` defaults to 50 and is clamped at 200 with a non-numeric or non-positive value a typed 400 `INVALID_REQUEST`; `offset` defaults to 0 with non-numeric or negative a 400; an unknown or deleting collection is a 404; newest first. `sourcePath`/`original_path`, `sha256` and `errorMessage` are never serialized, and the child chose to omit `errorMessage` entirely rather than reduce it, because it can carry document text — recorded. Controller verified the accumulated gate independently: 759 JVM tests, 0 failures, 0 errors, 0 skipped.

Task 23a-1: controller brief defect, corrected. My brief required a bearer credential on this read endpoint. The child challenged it and was right: `Security.kt:81-83` documents that read-only requests need no credential because they are same-origin and loopback-only, `Security.kt:73-78`/`:104-115` restrict the requirement to state-changing methods, and `SecurityTest.kt:170` proves it for `GET /api/collections`. The brief now requires the opposite and the test asserts the documented read semantics instead. Root cause recorded: I generalised from the 4b-3 review, where three Investigate endpoints correctly return 401 without a credential, but those are all POSTs and the guard is method-aware.

Task 23a-2: dispatched `e371989c` (worker `openrouter/deepseek/deepseek-v4-flash-0731:high`, fresh context, BASE `983e202`) with brief `task-23a2-brief.md` — `GET /api/jobs/{id}/events` as SSE, registered inside the already-guarded `configureRoutes` without touching the existing `/api/jobs` list/detail/items/cancel routes at `Routes.kt:194-240`. The brief binds: unknown job 404; emit the current state first so a client connecting mid-run is immediately correct; then an event on every state or progress change, polled from `JobStore.get` rather than an in-process pub/sub; every message carries an `id:` so `Last-Event-ID` can resume without re-sending what the client has; a final event then close on a terminal state; and a bounded injectable idle deadline so the stream cannot live forever and the test does not wait for it. Sanitised: job state, progress counters and error code only, never document text, a path or a secret. Six tests, written first.

Task 23a-1: task review dispatched in parallel with the 23a-2 writer — reviewer `openrouter/deepseek/deepseek-v4-pro` (run `c8a7ea71`, package `review-97bd14b..983e202.diff`, read-only against a fixed diff so it cannot collide with the writer). It must judge whether the raw-body assertion genuinely fails if a leaked field were serialized rather than merely passing today, whether non-numeric `limit` produces a typed 400 instead of reaching the generic handler, whether the ordering is stable enough that a row cannot repeat or vanish across pages, and whether `Security.kt`/`SecurityTest.kt` are untouched.

Task 23a-1: complete (`97bd14b`..`983e202`, review clean). Reviewer `openrouter/deepseek/deepseek-v4-pro` (run `c8a7ea71`) verdicted spec ✅ and task quality ✅ with **no Critical or Important findings**, and confirmed the properties I asked it to test adversarially rather than assume: the privacy assertion operates on the raw wire string and would fail if `sourcePath`, the serialized `original_path`, `sha256` or `errorMessage` leaked — the fixture deliberately inserts a non-null `errorMessage` and the test also asserts the literal text and a literal path are absent, so `explicitNulls = false` cannot hide a regression; non-numeric and non-positive `limit`/`offset` throw `BadRequestException`, which `call.handle` maps to a typed 400 at `Routes.kt:363-367` **before** the generic catch at `:375`, with the store's own `require` as a second safety net; `total` comes from `countByCollection` with no pagination and the test proves it constant at 205 across five different `limit`/`offset` combinations; and ordering is deterministic (`ORDER BY created_at DESC, id`), so a row cannot repeat or vanish across pages. It also confirmed `Security.kt` and `SecurityTest.kt` are untouched and that the corrected no-credential read semantics are what shipped.

Task 23a-1: minor (deferred) — the presence assertions in `DocumentRoutesTest.kt:113-126` do not assert that `id` is present even though the Svelte page depends on it for detail and source links, so a regression dropping `id` would not be caught. Carried into the next backend slice's dispatch.
Task 23a-1: minor (deferred) — `DocumentRoutes.kt` has no trailing newline, and an empty `?limit=` produces the terse message "was ''"; both style-only, `git diff --check` is clean.

Task 23a-2: first run `e371989c` hit the runner deadline and handed off BLOCKED-in-progress rather than timing out silently — the checkpoint mechanism worked. It produced a complete 227-line `JobEventRoutesTest.kt` with 7 tests (the brief's six plus one locking `Last-Event-ID` resumption) as an untracked file, and its red run failed for exactly the right reason: `compileTestKotlin` could not resolve `JobEventWire`, the type the unimplemented route must provide. Everything was researched and decided, including a genuinely useful verification against `ktor-http-jvm-3.2.0-sources.jar`: `HttpHeaders.LastEventID` does not exist in Ktor 3.2.0, so the literal `"Last-Event-ID"` header name must be used. It also pre-located that the existing `/api/jobs` list/detail/items/cancel block at `Routes.kt:194-240` must not be touched, and that a 4-segment `/api/jobs/{id}/events` route registers without conflicting.

Task 23a-2: resumed as `b9e33311` with its own implementation spec, plus two controller adjustments. First, the idle deadline resets on emit rather than on poll, which means a job that runs quietly for more than 30 seconds will close the stream and the client must reconnect with `Last-Event-ID`; that is acceptable because Task 23 Step 3 already requires reconnect and refetch after disconnect, but it is a real property of the contract, so the child must document it for the UI author rather than leave it implicit — and it was told not to silently widen the bound if it disagrees, but to state its reasoning. Second, `JobEventWire` must carry only `state`, `stage`, `completed`, `total`, `errorCode` and `cancelRequested`, never `errorMessage` or any document payload, because this stream is an unauthenticated read and therefore must not carry anything the redaction rules protect.

Task 23a-2: second run `b9e33311` timed out at 45 minutes with the implementation complete but uncommitted. Controller verified the code independently rather than reading its prose: `./gradlew test --tests 'infoscry.server.*'` is BUILD SUCCESSFUL with **65 tests, 0 failures, 0 errors, 0 skipped**, and `JobEventRoutesTest` is 7/7 green, with `JobEventRoutes.kt` (5,652 bytes) and its one-line registration in `Routes.kt` in the working tree.

Task 23a-2: process finding worth carrying to every remaining slice. The run died on verification, not on code: a single bash tool call carried a short deadline, `./gradlew check` needs six to ten minutes because it includes the frontend build, and the child worked around it with `nohup`, a background pid and `sleep` polling — which consumed the rest of its clock. Fix: dispatches whose work ends in `check` now pass `toolTimeoutMs` (25 minutes for the resume) so the command runs directly in one call, and the briefs instruct explicitly that `nohup`, `&`, `sleep` and polling loops are forbidden for verification. Earlier slices avoided this only because their `check` runs happened to fit inside the default deadline.

Task 23a-2: resumed as `b94e0604` with the raised per-tool deadline, instructed to run `check` once in a single call, then commit the three files, run `git diff --check`, and append the "Task 23a-2" report section recording the endpoint contract — including the verified Ktor finding that `HttpHeaders.LastEventID` does not exist in 3.2.0 so the literal `"Last-Event-ID"` header name is required — and the explicit consequence that a job quiet longer than the idle bound closes the stream, so the jobs page must reconnect with `Last-Event-ID` and refetch.

Task 23a-2: DONE at `4cfbea2` (`feat(server): stream job state and progress as server-sent events`), 3 files, 357 insertions. Endpoint contract: `GET /api/jobs/{id}/events`, an unauthenticated read, unknown id a 404; each event is `id: <seq>` plus `data:` with `JobEventWire(jobId, state, stage, completed, total, errorCode, cancelRequested)`, deliberately omitting `errorMessage` and any document payload so an unauthenticated stream carries nothing the redaction rules protect; the `id` is an opaque per-stream counter, a fresh stream starts at 1 with the current state, and `Last-Event-ID: N` continues the counter at N+1 while re-sending the current state and re-emitting nothing under an old id; Ktor 3.2.0 has no `HttpHeaders.LastEventID` so the literal header name is required; the idle bound is 30 s, injectable, reset on every emit and never on a poll, so a job quiet past the bound closes the stream and the jobs page must reconnect with `Last-Event-ID` and refetch; a terminal state closes the stream after the final event. Controller verified the gate independently on `4cfbea2`: `./gradlew check` BUILD SUCCESSFUL, **766 JVM tests, 0 failures, 0 errors, 0 skipped** (759 baseline plus 7), frontend 5/5. The child also disclosed that its first backgrounded `check` was invalidated by stale `build/test-results/test/binary` state left by an earlier killed run until `cleanTest` cleared it, and reported the clean run as authoritative — a good catch, and a reason to distrust a backgrounded gate count.

Task 23: paused here by operator instruction ("finish after this and document progress"). **Outstanding: the Task 23a-2 task review has not run**; the slice is committed and gate-green but not accepted. That review is the first action for the next session, before any further slice. Next slices after it: 23a-3 (settings, LLM profiles, prompts, OCR languages, sanitized log events, reusing `LogRecord`/`LogFilter`/`LogReader` at `logging/`), then the four Svelte slices.

Task 23: status doc updated and this ledger committed as the pause record. `docs/implementation-status.md` now carries the Task 23 row with both slices, the corrected auth rule (reads need no credential), the `evidence`-on-`done` contract that unblocks citations in the UI, the 766-test baseline, the pending 23a-2 review as the explicit first action, and the `toolTimeoutMs` process finding for `check` runs.
